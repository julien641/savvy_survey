package com.savvy.savvy_survey;

public class backup {

/*

         view=Frag_1_Tools.settingupspinners(
                getActivity().getApplicationContext(),
                view,
                R.id.inspectors,
                inspector.result,
                R.layout.spinner_item
        );

        view = Frag_1_Tools.settingupspinners(
                getActivity().getApplicationContext(),
                view,
                R.id.Location,
               location.result,
                R.layout.spinner_item
        );
        view= Frag_1_Tools.settingupspinners(
                getActivity().getApplicationContext(),
                view,
                R.id.unit_size,
               size.result,
                R.layout.spinner_item

        );
*/
}
